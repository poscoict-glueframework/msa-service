server:
  port: 8080
spring:
  application:
    name: zuul-proxy
#  cloud:
#    config:
#      uri: http://localhost:8888
#      fail-fast: true
eureka:
  client:
    enabled: true
    serverUrl:
      defaultZone: http://localhost:8761/eureka/
